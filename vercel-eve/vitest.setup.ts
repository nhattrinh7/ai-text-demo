import '@testing-library/jest-dom';

// thư viện @testing-library/jest-dom cung cấp các hàm rất tốt cho test giao diện như:
// expect(button).toBeVisible() (kiểm tra nút có hiển thị không) hay expect(input).toBeDisabled() (kiểm tra ô nhập có bị vô hiệu hóa không)
// ta import thư viện này để dùng thêm được các hàm tiện ích của thư viện này
