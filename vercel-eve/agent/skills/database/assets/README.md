# Database Assets

<!--
GHI CHÚ:
Thư mục 'assets' thường được dùng để chứa các tài nguyên tĩnh như hình ảnh (sơ đồ ERD, kiến trúc database), các file JSON hoặc CSV chứa dữ liệu mẫu để AI dùng khi cần thiết.
Khi cần, AI hoặc các tool khác có thể đọc từ đây bằng hàm `ctx.getSkill("database").file("assets/...")`.
-->

- `schema_erd.png`: Hình ảnh sơ đồ quan hệ các bảng (Database ERD).
- `sample_data.json`: File dữ liệu mẫu của các bảng.
