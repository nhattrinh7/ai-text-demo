version: '3.8'

services:
  my-rag:
    build: 
      context: ./my-rag
    container_name: my-rag
    restart: always
    env_file:
      - ./my-rag/.env  # Đọc biến môi trường như OPENAI_API_KEY từ my-rag/.env

  mcp-server:
    build: 
      context: ./mcp-server
    container_name: mcp-server
    ports:
      - "3001:3001"
    restart: always
    env_file:
      - ./mcp-server/.env
    depends_on:
      - my-rag
